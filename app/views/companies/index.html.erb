<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Companies</title>
  </head>
    <body>
      <h3>All Companies</h3>
      <h4><%= link_to "Add a company here!", new_company_path, class:"co button" %></h4>
          <ul class="company-container">
            <h4>Click on the name of the company to see all jobs</h4>
          <% @companies.each do |company| %>
            <h4><%= link_to company.name, company_path(company) %>:</h4>
            <%= link_to "Edit", edit_company_path(company), class:"button" %>
            <%= link_to "Delete", company_path(company), method: "delete", class:"button" %>
          <% end %>
          </ul>
    </body>
</html>
