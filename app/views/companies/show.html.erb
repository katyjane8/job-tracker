
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Jobs</title>
  </head>
    <body>
      <h3> All Jobs for <strong><%= @company.name %></strong></h3>
        <% @company.jobs.each do |job| %>
        <ul><%= job.title %></ul>
        <% end %>
    </body>
</html>
